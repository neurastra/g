int whisky = 100;

// Указатель (может менять адрес)
int* ptr = &whisky;
*ptr = 200; // изменяем значение
ptr = nullprt; // можем обнулить

// Ссылка (всегда к одному объекту)
int& ref = whisky;
ref = 300; // изменяем значение
// ref = nullptr; нельзя - всегда привязана.
